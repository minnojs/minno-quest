/**
 * Task manager:
 *
 * 1. loads a task object.
 * 2. $compile the appropriate script and apply the appropriate scope. 
 */
define([],function(){
	describe('getResource', function(){
		// takes url
		// returns $q
		// broadcasts when the object is here? - probably better to trust the $q
		
	});
});
